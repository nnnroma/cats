export interface IBreed {
    id: string,
    name: string,
    image: {
        height: number,
        id: string,
        url: string,
        width: number
    }
}

export interface ICat {
    url: string
}
